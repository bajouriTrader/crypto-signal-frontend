V.2.10.6

1) مارجین ریل = ۲۰٪ موجودی آزاد (سقف نرم ۳۰٪، سقف سخت ۴۰ USDT)
   دیگر سقف ثابت ۶ دلار نیست — با بالانس ۱۰۰ → مارجین ~۲۰

2) دانلود گزارش: فقط نسخه‌های اخیر + حداکثر ۸۰۰ ردیف
   آرشیو کامل: /demo-trade/export?limit=5000&current_only=false

3) پنل گزارش: stats آخرین ۱۲۰۰ معامله، جدول نسخه فقط شاخه فعلی،
   سوابق تحلیل ۱۰، دمو صفحه اول ۱۵۰

بک‌اند: real_trade.py, app.py, version.py → Relaunch
فرانت: reportExport.js, AdminPanel.jsx, App.jsx → push + hard refresh
